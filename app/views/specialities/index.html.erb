<h1>Список специальностей</h1>

<table>
  <tr>
    <th>Название</th>
    <th>Код специальности</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @specialities.each do |speciality| %>
  <tr>
    <td><%= speciality.name %></td>
    <td><%= speciality.key_specialiti %></td>
    <td><%= link_to 'Показать', speciality %></td>
    <td><%= link_to 'Изменить', edit_speciality_path(speciality) %></td>
    <td><%= link_to 'Удалить', speciality, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Новая специальность', new_speciality_path %>
